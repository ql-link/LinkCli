# <KEY> · 人工端到端验收记录

> 本文件只记录当前任务的人工执行步骤和真实结果。不得把计划、组件测试或后端接口测试写成人工端到端已通过。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 关联需求 | `<KEY>` / `<Issue 链接或编号>` |
| 验收契约 | `.specs/<KEY>/acceptance.feature` |
| 分支或提交 | `<开始执行人工验收时的分支名或提交哈希>` |
| 可提交内容状态 | `<开始执行时是否有未提交改动；不得填密钥或真实数据>` |
| 执行环境 | `<本地/测试环境；浏览器；关键服务版本>` |
| 执行人 | `<姓名或角色>` |
| 执行时间 | `<YYYY-MM-DD HH:mm 时区>` |
| 总体状态 | `未执行 / 通过 / 失败 / 阻塞` |

## 2. 环境与测试数据

- 启动方式：`<实际命令>`
- 服务状态：`<Web / FastAPI / MySQL / MinIO 中实际涉及的部分>`
- 虚构账号或数据：`<只写非敏感标识，不写密码、Cookie、真实联系方式或密钥>`
- 其他前置条件：`<没有则写“无”>`

## 3. 清单汇总

| 编号 | Acceptance 场景 | 验证面 | 状态 | 证据或备注 |
| --- | --- | --- | --- | --- |
| MAN-01 | `<Scenario 名称>` | `<跨端/视觉/上传下载/权限/回归>` | 未执行 |  |

状态只允许：`未执行`、`通过`、`失败`、`阻塞`、`不适用`。

## 4. 详细步骤

### MAN-01 · <场景名称>

- 来源：`acceptance.feature` 中 `<Scenario 名称>`
- 前置条件：<可实际建立的状态>
- 操作步骤：
  1. <单个用户动作>
  2. <单个用户动作>
- 预期结果：
  1. <具体可观察结果>
  2. <具体可观察结果>
- 实际结果：<执行后填写；未执行时保留“未执行”>
- 状态：`未执行`
- 证据：<截图、日志或录屏路径；没有则写“无”>
- 备注：<失败复现条件、环境阻塞或“不适用”理由>

## 5. 失败与阻塞

| 编号 | 类型 | 实际影响 | 复现条件 | 下一步或关联缺陷 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

没有失败或阻塞时写“无”。

## 6. 验收结论

- 统计：通过 `<0>`；失败 `<0>`；阻塞 `<0>`；未执行 `<0>`；不适用 `<0>`。
- 自动化验证：`<实际成功命令及结果>`
- 人工验收结论：`未执行 / 通过 / 失败 / 阻塞`
- 未覆盖范围：<没有则写“无”>
- 是否可以进入质量审查：`是 / 否`
- 结论依据：<简述证据，不能只写“测试正常”>
