# <KEY> · <功能名称>实施报告

> 面向接手开发者、审查者和产品负责人说明实际交付。使用完成时和具体事实，不重复完整方案文档，不使用“完成相关逻辑”“优化体验”等抽象表述。仅在确有内容时保留可选章节。

## 1. 实现结果

<用 2–4 段直接说明本次最终做成了什么。先写用户或调用方现在获得的能力，再写明确边界：哪些行为已经支持、失败时怎样表现、哪些内容没有实现。>

### <实际交付的能力名称>

<按产品能力或运行职责说明真实行为。写清触发入口、处理结果、错误反馈、数据是否保存，以及对现有功能是否有影响。一个能力讲清楚后再写下一个，不按“代码 / 配置 / 数据 / 测试”机械分类。>

主要实现位置：

- `<真实文件路径>`：<该文件本次承担的具体职责>
- `<真实文件路径>`：<该文件本次承担的具体职责>

## 2. 实际运行方式

<从真实触发动作开始，用自然语言说明实现后的主链路。涉及前端、后端、数据库、对象存储或外部服务时，明确数据经过哪些边界、在哪里校验、成功和失败分别产生什么结果。>

1. <触发者执行的真实动作>
2. <系统执行的关键校验或数据读取>
3. <核心处理和依赖调用>
4. <成功时的可见结果或持久化结果>
5. <失败时的反馈，以及是否产生副作用>

## 3. 与确认方案的差异（按需保留）

<只有实际实现与已冻结方案文档或验收契约存在差异时保留本节。逐项说明原方案、实际实现、改变原因、用户或系统影响，以及相关验证。没有差异时删除整节，不保留“无”表格。>

### <差异名称>

原方案计划<具体方案>，实际实现改为<具体做法>。调整原因是<真实原因>。这会导致<用户行为、契约、数据、兼容或运维影响>，已经通过<验证方式>确认。<如有后续事项，说明何时和在什么条件下处理。>

## 4. 验证结果

实际执行：

- `<真实命令>`：<退出结果、通过数量、构建结果或关键警告>
- `<真实命令>`：<退出结果、通过数量、构建结果或关键警告>

已经覆盖：

- <主流程或关键业务规则>
- <权限、失败、边界、迁移或回归行为>

人工验收：<不适用；或说明环境、执行结果和证据位置。>

未覆盖：<没有则写“无”；存在时写清具体行为、原因和风险，不能用“其他场景”概括。>

## 5. 当前限制与 PR 重点

当前仍存在以下限制：

- <本次范围内确认接受的限制及实际影响；没有则删除本段>

PR 审查时重点确认：

- <需要审查者核实的具体产品行为、契约、权限、数据、兼容或部署事项>
- <需要审查者核实的具体风险>
