# MCPSTAT-1-CONSOLE · 人工端到端验收记录

> 本文件只记录当前任务的人工执行步骤和真实结果。不得把计划、组件测试或后端接口测试写成人工端到端已通过。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 关联需求 | `MCPSTAT-1-CONSOLE` |
| 验收依据 | `.specs/MCPSTAT-1-CONSOLE/solution.md` 版本详情差异契约 |
| 分支或提交 | `master` / `9f52af4` |
| 可提交内容状态 | 开始执行时存在 18 项未提交修改，均属于当前控制台交付范围或既有工作区内容 |
| 执行环境 | 本地；Codex 内置浏览器；Vite `127.0.0.1:5173`；LinkCli API `127.0.0.1:3000`；开发 MySQL；标准 MCP Mock `127.0.0.1:3200/mcp` |
| 执行人 | Codex |
| 执行时间 | 2026-08-06 19:06 CST |
| 总体状态 | 通过 |

## 2. 环境与测试数据

- 启动方式：前端使用 `npm run dev:web`，API 使用加载 `.env.development.local` 后的 `npm run dev:api`，标准 MCP Mock 使用当前会话中的本地测试服务。
- 服务状态：Web、API、开发 MySQL 和标准 MCP Mock 均可用。
- 虚构账号或数据：审核员 `e2e_review_13723576`；项目 `uie2e13723576`；已批准版本 v1；工具 `echo`。
- 其他前置条件：v1 已通过审核并成为当前生效版本。

## 3. 清单汇总

| 编号 | 验收场景 | 验证面 | 状态 | 证据或备注 |
| --- | --- | --- | --- | --- |
| MAN-01 | 已批准首版保留发布前工具差异 | 跨端 / 页面回归 | 通过 | 版本详情页显示工具 `echo` 的差异为“新增”，浏览器错误日志为空 |

## 4. 详细步骤

### MAN-01 · 已批准首版保留发布前工具差异

- 来源：`solution.md` 中 `/api/versions/:versionId` 差异基线契约。
- 前置条件：项目 v1 已批准并发布，v1 是当前生效版本，审核员账号可查看版本详情。
- 操作步骤：
  1. 在登录页使用虚构审核员账号登录。
  2. 打开项目 v1 的版本详情页。
  3. 查看“工具差异”区域和浏览器错误日志。
- 预期结果：
  1. 页面显示版本状态“已批准”。
  2. 工具 `echo` 的差异显示“新增”，不显示 `unchanged`。
  3. 页面加载期间没有浏览器错误日志。
- 实际结果：页面显示“已批准”，`echo` 显示“新增”，浏览器错误日志为空。
- 状态：`通过`
- 证据：验收完成后浏览器保留在 `http://127.0.0.1:5173/versions/556b4b64-1c9c-40a5-a547-a10311e07f1f`。
- 备注：页面使用修复后的后端版本详情响应，未修改测试数据。

## 5. 失败与阻塞

无。

## 6. 验收结论

- 统计：通过 `1`；失败 `0`；阻塞 `0`；未执行 `0`；不适用 `0`。
- 自动化验证：`npx vitest run tests/auth-http.integration.test.ts` 通过，4 项测试通过；完整门禁由后续 `spec verify` 执行。
- 人工验收结论：`通过`
- 未覆盖范围：未重复执行注册、角色初始化、项目登记和真实 MCP 调用全流程；这些流程在修复前已通过，本次生产改动仅影响版本详情差异基线选择。
- 是否可以进入质量审查：`是`
- 结论依据：真实浏览器读取已批准 v1 的版本详情，确认差异由 `unchanged` 恢复为“新增”，且页面没有运行错误。
